var first=prompt("Enter the first  number");

first=parseInt(first);

var diff=prompt("Enter the Comman Difference");

diff=parseInt(diff);

var num=prompt("Enter the Nth number");

num=parseInt(num);

var result= first + (num-1) * diff;

alert(result);
